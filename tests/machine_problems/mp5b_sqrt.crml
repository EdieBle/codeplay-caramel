bean cup() [
    drip n = 0.0
    glaze("Enter a number: ")
    batter@ n
    ifbrew (n <= 0) {
        glaze("Error: Invalid Input\n")
        refill? 0
    }
    drip guess = n / 2.0
    pour (bean i = 0; i < 20; i++) {
        guess = (guess + n / guess) / 2.0
    }
    bean result = guess
    drip check = result
    glaze("Square root: ")
    ifbrew (check == guess) {
        glaze(result)
    }
    ifbrew (check != guess) {
        glaze(guess)
    }
    glaze("\n")
    refill? 0
]
