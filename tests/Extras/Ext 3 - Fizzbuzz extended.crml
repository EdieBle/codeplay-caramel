bean cup() [
	bean fizzinp
	batter@fizzinp("Insert Value for FizzBuzz Program: ")
	
	glaze("============= Fizzbuzz result from 1 to your input =============\n")
	pour(bean j = 1; j <= fizzinp; j++) {
		ifbrew(j % 3 == 0 && j % 5 == 0 && j % 7 == 0) {
			glaze("Whiz\n")
			skip
		} elifroth (j % 3 == 0 && !(j % 5 == 0)) { 
			glaze("Fizz\n")
			skip	
		} elifroth (!(j % 3 == 0) && j % 5 == 0) {
			glaze("Buzz\n")
			skip
		} elifroth (j % 3 == 0 && j % 5 == 0) {
			glaze("FizzBuzz\n")
			skip
		}
		glaze(j + "\n")
	}
  refill? 0
]